Spring MVC ：

1.Spring MVC 核心流程与消息转换器

（1）四大核心组件：

DispatcherServlet（前端控制器——总调度）

HandlerMapping（处理器映射器——请求URL对应到具体的负责人）

​															 POST  http://local:8080/api/user/register）

HandlerAdapter（处理器适配器——负责人具体完成事情）

HttpMessageConverter（消息转换器——JSON/XML

​																        统一格式）

完整请求流程：用户发送HTTP请求+DispatcherServlet 接收请求+HandlerMapping 查找对应 Controller 方法＋HandlerAdapter执行方法＋HttpMessgerConverter消息转换器转换返回值格式(JSON)→DispatcherServlet 将结果响应给前端。

2.高级参数绑定与分组校验

3.全局异常处理与拦截器

4.文件上传下载 + 跨域配置



**JPA的逆向工程特性**，即通过Java实体自动生成数据库表

MyBatis 正向工程（先建库表再生成代码）

![image-20260402153837992](C:\Users\Lenovo\AppData\Roaming\Typora\typora-user-images\image-20260402153837992.png)